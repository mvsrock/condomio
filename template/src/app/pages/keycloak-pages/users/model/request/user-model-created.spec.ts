import { UserModelCreated } from './user-model-created';

describe('UserModelCreated', () => {
    it('should create an instance', () => {
        expect(new UserModelCreated()).toBeTruthy();
    });
});
